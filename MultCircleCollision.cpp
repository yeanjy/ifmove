#include "MultiCircleCollision.h"

mCircles::mCircles()
{}

bool mCircles::wallDetection(int height, int width)
{
    for (int i = 0; i < myCircles.size(); i++)
    {
        if (myCircles[i].getcX >= width/2)
    }
}